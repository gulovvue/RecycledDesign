# Portfolio UI Kit

A high-fidelity recreation of the Recycling Portfolio landing page (`Данилов М.Ф. — 1-МГ-50`).

- `index.html` — full landing page demo, scrollable, with sticky nav, hero, 4 project sections, footer.
- `Nav.jsx`, `Hero.jsx`, `ProjectHeader.jsx`, `SpecTable.jsx`, `ImageCard.jsx`, `AccentSlab.jsx`, `Gallery.jsx`, `Footer.jsx` — reusable component pieces.

All components consume tokens from `../../colors_and_type.css`. Real project photography is **not provided** — placeholders read `Hero · Стойка для растений` etc. Drop real images into the relevant `<img src>` to finish.
